import 'package:flutter/widgets.dart';

/// Platzhalter für visuelles Hover-Feedback über Tiles.
/// Wird im nächsten Schritt mit Leben gefüllt.
class DragColorOverlay extends StatelessWidget {
  const DragColorOverlay({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
