class LocalWordWorldMembership {
  const LocalWordWorldMembership({
    required this.wordId,
    required this.categoryId,
    required this.createdAt,
  });

  final String wordId;
  final String categoryId;
  final DateTime createdAt;
}
